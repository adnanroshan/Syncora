# Code On Time backend that hosts /v2 and /oauth2/v2.
# No trailing slash. Leave EMPTY to run in mock mode (no network, no OAuth).
VITE_BACKEND_URL=http://localhost:51780

# OAuth 2.0 client_id you obtained when registering this SPA under
# /oauth2/v2/apps (see README — section "Register the SPA").
VITE_CLIENT_ID=IsKtprsgh36x4rKPTXdJGvsPz0zLLADOsJfzaB1liYE

# Optional — override the default OAuth scopes.
# VITE_OAUTH_SCOPE=offline_access openid profile email
